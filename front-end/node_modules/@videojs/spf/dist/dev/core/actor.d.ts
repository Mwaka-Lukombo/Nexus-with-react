import { ReadonlySignal } from "./signals/primitives.js";

//#region src/core/actor.d.ts
/** Complete actor snapshot: finite status + non-finite context. */
interface ActorSnapshot<Status extends string, Context> {
  status: Status;
  context: Context;
}
/** Generic actor interface: owns its snapshot and notifies observers. */
interface SignalActor<Status extends string, Context> {
  /** Current snapshot. Readable and reactive; not writable by consumers. */
  readonly snapshot: ReadonlySignal<ActorSnapshot<Status, Context>>;
  /** Tear down the actor. */
  destroy(): void;
}
//#endregion
export { SignalActor };
//# sourceMappingURL=actor.d.ts.map