import { Constructor } from "@videojs/utils/types";

//#region src/core/media/delegate.d.ts
interface Delegate {
  attach?(target: EventTarget): void;
  detach?(): void;
}
interface BaseType extends EventTarget {
  attach?(target: EventTarget): void;
  detach?(): void;
  get?(prop: string): any;
  set?(prop: string, val: any): void;
  call?(prop: string, ...args: any[]): any;
}
/**
 * Mixin that intercepts `get`, `set`, and `call` to delegate property access
 * and method calls to an instance of `DelegateClass` before falling through
 * to the base class implementation.
 *
 * Works with both `CustomMediaMixin` and `ProxyMixin`.
 */
declare function DelegateMixin<Base extends Constructor<BaseType>, D extends Constructor<Delegate>>(BaseClass: Base, DelegateClass: D): Constructor<InstanceType<Base> & InstanceType<D>> & Omit<Base, "prototype">;
//#endregion
export { BaseType, Delegate, DelegateMixin };
//# sourceMappingURL=delegate.d.ts.map