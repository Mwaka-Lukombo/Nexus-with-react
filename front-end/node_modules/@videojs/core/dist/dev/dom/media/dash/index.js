import { DelegateMixin } from "../../../core/media/delegate.js";
import { VideoProxy } from "../proxy.js";
import { CustomVideoElement } from "../custom-media-element/index.js";
import * as dashjs from "dashjs";
//#region src/dom/media/dash/index.ts
var DashMediaDelegate = class {
	#engine;
	#src = "";
	constructor() {
		this.#engine = dashjs.MediaPlayer().create();
		this.#engine.initialize(void 0, void 0, false);
	}
	get engine() {
		return this.#engine;
	}
	attach(target) {
		this.#engine.attachView(target);
	}
	detach() {
		this.#engine.attachView(null);
	}
	destroy() {
		this.#engine.destroy();
	}
	set src(src) {
		this.#src = src;
		this.#engine.attachSource(src);
	}
	get src() {
		return this.#src;
	}
};
var DashCustomMedia = class extends DelegateMixin(CustomVideoElement, DashMediaDelegate) {};
var DashMedia = class extends DelegateMixin(VideoProxy, DashMediaDelegate) {};
//#endregion
export { DashCustomMedia, DashMedia, DashMediaDelegate };

//# sourceMappingURL=index.js.map