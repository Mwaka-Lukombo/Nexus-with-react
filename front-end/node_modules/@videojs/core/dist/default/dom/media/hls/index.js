import { bridgeEvents } from "../../../core/utils/bridge-events.js";
import { DelegateMixin } from "../../../core/media/delegate.js";
import { VideoProxy } from "../proxy.js";
import { CustomVideoElement } from "../custom-media-element/index.js";
import { NativeHlsMediaDelegate } from "../native-hls/index.js";
import { HlsJsMediaDelegate } from "./hlsjs.js";
import { shallowEqual } from "@videojs/utils/object";
import Hls from "hls.js";
//#region src/dom/media/hls/index.ts
const PlaybackTypes = {
	MSE: "mse",
	NATIVE: "native"
};
const SourceTypes = {
	M3U8: "application/vnd.apple.mpegurl",
	MP4: "video/mp4"
};
var HlsMediaDelegate = class extends EventTarget {
	#target = null;
	#delegate = null;
	#src = "";
	#type;
	#preferPlayback = "mse";
	#config = {};
	#debug = false;
	#preload = "metadata";
	#loadRequested;
	#prevEngineProps;
	get target() {
		return this.#target;
	}
	get engine() {
		return this.#delegate?.engine ?? null;
	}
	get error() {
		return this.#delegate?.error ?? null;
	}
	get src() {
		return this.#src;
	}
	set src(src) {
		this.#src = src;
		this.#requestLoad();
	}
	/** Explicit source type. When unset, inferred from the source URL extension. */
	get type() {
		return this.#type ?? inferSourceType(this.src);
	}
	set type(value) {
		this.#type = value;
		this.#requestLoad();
	}
	/** Whether to prefer `'mse'` (hls.js) or `'native'` (browser-built-in) HLS. */
	get preferPlayback() {
		return this.#preferPlayback;
	}
	set preferPlayback(value) {
		this.#preferPlayback = value;
		this.#requestLoad();
	}
	get config() {
		return this.#config;
	}
	set config(config) {
		this.#config = config;
		this.#requestLoad();
	}
	get debug() {
		return this.#debug;
	}
	set debug(debug) {
		this.#debug = debug;
		this.#requestLoad();
	}
	get preload() {
		return this.#preload;
	}
	set preload(value) {
		this.#preload = value;
		if (this.#delegate) this.#delegate.preload = value;
	}
	attach(target) {
		this.#target = target;
		this.#delegate?.attach(target);
	}
	detach() {
		this.#target = null;
		this.#delegate?.detach();
	}
	destroy() {
		this.#engineDestroy();
		this.detach();
	}
	load() {
		this.#loadRequested = null;
		if (this.#shouldEngineUpdate(this.#engineProps())) {
			this.#engineDestroy();
			this.#prevEngineProps = this.#engineProps();
			this.#delegate = Hls.isSupported() && this.type === SourceTypes.M3U8 && this.preferPlayback !== PlaybackTypes.NATIVE ? new HlsJsMediaDelegate({ config: {
				...this.config,
				debug: this.debug
			} }) : new NativeHlsMediaDelegate();
			bridgeEvents(this.#delegate, this);
			if (this.target) this.#delegate.attach(this.target);
			this.#delegate.preload = this.preload;
		}
		if (this.#delegate) this.#delegate.src = this.#src;
	}
	async #requestLoad() {
		if (this.#loadRequested) return;
		await (this.#loadRequested = Promise.resolve());
		this.#loadRequested = null;
		this.load();
	}
	#shouldEngineUpdate(nextEngineProps) {
		return !shallowEqual(this.#prevEngineProps, nextEngineProps);
	}
	#engineProps() {
		return {
			config: this.config,
			debug: this.debug,
			preferPlayback: this.preferPlayback,
			type: this.type
		};
	}
	#engineDestroy() {
		this.#delegate?.destroy();
		this.#delegate = null;
		this.#prevEngineProps = null;
		this.#loadRequested = null;
	}
};
function inferSourceType(src) {
	if ((src.split(/[?#]/)[0] ?? "").endsWith(".mp4")) return SourceTypes.MP4;
	return SourceTypes.M3U8;
}
var HlsCustomMedia = class extends DelegateMixin(CustomVideoElement, HlsMediaDelegate) {};
var HlsMedia = class extends DelegateMixin(VideoProxy, HlsMediaDelegate) {};
//#endregion
export { Hls, HlsCustomMedia, HlsMedia, HlsMediaDelegate, PlaybackTypes, SourceTypes };

//# sourceMappingURL=index.js.map